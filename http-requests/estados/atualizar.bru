meta {
  name: atualizar
  type: http
  seq: 4
}

put {
  url: {{BASE_URL}}/estados/{{id}}
  body: json
  auth: none
}

headers {
  ~Accept: application/xml
}

body:json {
  {
    "nome": "Rio Grande do Sul"
  }
}

vars:pre-request {
  id: 4
}

assert {
  res.body['nome']: eq Tailandesa
  res.status: eq 200
}
